application:
  deploy:
    environment: "test"

spring:
  jpa:
    hibernate:
      ddl-auto: validate
  datasource:
    url: jdbc:mysql://${DATABASE_MYSQL_HOST}/${DATABASE_MYSQL_SCHEMA:bellsafehouse_test}?${DATABASE_MYSQL_OPTION:charset=utf8mb4,utf8&createDatabaseIfNotExist=true}

security:
  jwt:
    signature-key: ${SECURITY_JWT_SIGNATURE_KEY:123456789012345678901234567890123456789012345678901234567890123456789012345678901234567890}
